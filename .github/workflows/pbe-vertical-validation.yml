name: PBE Vertical Validation

on:
  push:
    branches:
      - analysis/pbe-vertical-validation-20260920
    paths:
      - validation/pbe_vertical_compare.py
      - .github/workflows/pbe-vertical-validation.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  compare:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Check out analysis branch
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install LaCurent dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r commercial/requirements.txt

      - name: Install pinned PyBuildingEnergy
        run: |
          python -m pip install "git+https://github.com/EURAC-EEBgroup/pyBuildingEnergy.git@32ff8648312fc4cd0ac4e963464ee956a7c47d2d"

      - name: Run vertical comparison
        env:
          VALIDATION_OUT: validation-results
          MPLBACKEND: Agg
          PYTHONPATH: .
        run: |
          python validation/pbe_vertical_compare.py

      - name: Upload validation report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pbe-vertical-validation
          path: validation-results/
          if-no-files-found: warn
